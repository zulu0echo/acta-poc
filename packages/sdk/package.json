{
  "name": "@acta/sdk",
  "version": "0.3.0",
  "description": "ACTA — Anonymous Credentials for Trustless Agents. Single integration surface for issuer, holder, verifier, and predicates.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "TS_NODE_TRANSPILE_ONLY=true mocha --require ts-node/register 'test/**/*.ts' --timeout 30000"
  },
  "dependencies": {
    "@acta/shared": "*",
    "ethers": "^6.11.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.12.7",
    "chai": "^4.4.1",
    "mocha": "^10.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
